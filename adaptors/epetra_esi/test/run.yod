#!/bin/sh

#
#This script runs an mpi program using yod on ASCI red.
#
# Usage: run.yod <np> "executable [args]"
#
#note1: if your executable doesn't need any arguments, then you don't
#      need the quotes. If it does need arguments, then you do.
#      <np> is number-of-processors
#

(yod -sz ${1} ${2})

exit ${?}

