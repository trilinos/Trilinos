TRIBITS_PACKAGE(TriBITS)

TRIBITS_ADD_TEST_DIRECTORIES(python package_arch)

ASSERT_DEFINED(CMAKE_HOST_SYSTEM_NAME)
IF (CMAKE_HOST_SYSTEM_NAME)
  TRIBITS_ADD_TEST_DIRECTORIES(ctest/UnitTests)
ENDIF()

TRIBITS_PACKAGE_POSTPROCESS()
