# The Trilinos project must define its strategy for defining regression email
# addressses for all packages so that extra repos will pick up this policy by
# default
SET_DEFAULT(Trilinos_PROJECT_EMAIL_URL_ADDRESS_BASE software.sandia.gov)
