name: C/C++ CI

on: [workflow_dispatch]

jobs:
  build:

    runs-on: ubuntu-latest

    steps:
    - uses: actions/checkout@v3
    - name: echos
      run: pwd
    - name: echos
      run: env
    - name: Build CMake
      run: sudo cmake .
