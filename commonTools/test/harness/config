#
# herouxsmp - nightly
#

RUN {

    MACHINE             = herouxsmp;
    BUILD-DIR           = SERIAL-1;
    COMM                = serial;
    CVS-UPDATE-COMMAND  = "cvs -d :ext:mnphenow@software.sandia.gov:/space/CVS update -dP";
    INVOKE-CONFIGURE    = ic-serial-1;
    MAKE-COMMAND        = "make -j4";
    RECOVER             = YES;
    TEST-CATEGORY       = FRAMEWORK;
    MAX-PROC            = 16;
    MPI-START           = lamboot;
    MPI-STOP            = lamhalt;
    OUTPUT-DIR          = results;
    TEST-DATA-DIR       = /home/user/TrilinosTestData;
    COMMIT-RESULTS      = YES;
    VERBOSITY           = 1;
    LOG-VERBOSITY       = 1;
    SCHEDULE            = (MWFN, 23:00);
    SCHEDULE            = (TRS, 4:00);

}