#!/bin/bash
cmd="@BUILD_STAT_COMPILER_WRAPPER_INNER_COMPILER@"
if [ "${CMAKE_IS_IN_CONFIGURE_MODE}" == "1" ]; then
  ${cmd} "$@"
else
  "@BUILD_STATS_SRC_DIR@/wrapper/magic_wrapper.py" "----op=${cmd}" "$@"
fi
