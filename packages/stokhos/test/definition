PACKAGE_NAME=Stokhos

#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL) {
    DIRS = Hermite;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

(DOCUMENTATION) {
    TESTS = ../example/generate-sample-output.pl;
    COMM = SERIAL;
}
