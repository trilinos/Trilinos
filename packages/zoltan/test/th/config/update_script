#!/bin/sh
#
# Checkout the Acro 'data' directory that is used to commit testing
# result files that can be exacted by the DB daemon on software.sandia.gov.
#
# Note: this script will be run from the test-harness directory.
#
if test -d data ; then 
   echo ""
else
   cvs.z -Q update -d data
fi
