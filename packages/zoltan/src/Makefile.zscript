include /home/u/kddevin/code/Zoltan/Makefiles/Makefile.$(ZOLTAN_ARCH)


zoltan:	
	echo $(AR) $(RANLIB)
	zoltan_script \
	  '$(DEFS)' \
	  '$(CC)' \
	  '$(CFLAGS)' \
	  '$(F90CFLAGS)' \
	  '$(INCLUDE_PATH)' \
	  '$(PARMETIS_LIBPATH)' \
	  '$(JOSTLE_LIBPATH)' \
	  '$(JOSTLE_INCPATH)' \
	  '$(AR)' \
	  '$(RANLIB)' \
	  '$(ZOLTAN_ARCH)' \
	  '$(ZOLTAN_LIB_DIR)'

