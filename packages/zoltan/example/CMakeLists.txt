# Required modules


# Example definitions

ADD_SUBDIRECTORY(C)

IF (${PROJECT_NAME}_ENABLE_CXX)
ADD_SUBDIRECTORY(CPP)
ENDIF()

IF (${PROJECT_NAME}_ENABLE_Fortran AND Zoltan_ENABLE_F90INTERFACE)
ADD_SUBDIRECTORY(Fortran)
ENDIF()
