'plantUML input file
'Class diagram for IdentifierMap
'
@startuml

class "IdentifierMap<LNO, GNO, AppGID>" as Map

class Map{
Tpetra::MultiVector<AppGID, LNO, GNO> gid
Tpetra::MultiVector<int, LNO, GNO> value
void update()
void find()
void gidToGno()
void gnoToLno()
void gnoToGid()
void etc()
}

note "The IdentifierMap manages the global\nspace of application global IDs,\nZoltan global numbers, and\nprocess local IDs.\n" as N1


@enduml

