'plantUML input file
'Class diagram for Objective
'

@startuml

class "Objective<LNO, GNO, AppGID>" as Objective

class Objective{
IdentifierMap<LNO, GNO, AppGID> gid
vector<float> vertexWeights
vector<float> partSizes
vector<GNO> fixedVertices

virtual void setVertexWeights() 
virtual void getVertexWeights() 
virtual void setPartSizes() 
virtual void getPartSizes() 
virtual void setFixedVertices() 
virtual void getFixedVertices() 
virtual void minimizeNumberOfNeighbors()
virtual void minimizeCutWeight()
virtual void migrationBalanceTradeoff()

virtual void etc()
}

note  "The Objective describes\nthe problem to be solved." as N1

note left of Objective: Fixed vertices are really a\nconstraint. If we have\nmany problem characteristics\nlike this, it may add clarity\nto have a Constraint class.

Objective <|-- "PartitioningObjective<>"
Objective <|-- "ColoringObjective<>"
Objective <|-- "OrderingObjective<>"

@enduml

