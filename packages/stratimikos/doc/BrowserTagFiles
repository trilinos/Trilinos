@INCLUDE=thyra/doc/BrowserTagFiles # Requires @INCLUDE_PAH=$(TRILINOS_HOME)/packages
@INCLUDE=amesos/doc/BrowserTagFiles # Requires @INCLUDE_PAH=$(TRILINOS_HOME)/packages
@INCLUDE=aztecoo/doc/BrowserTagFiles # Requires @INCLUDE_PAH=$(TRILINOS_HOME)/packages
@INCLUDE=belos/doc/BrowserTagFiles # Requires @INCLUDE_PAH=$(TRILINOS_HOME)/packages
@INCLUDE=ifpack/doc/BrowserTagFiles # Requires @INCLUDE_PAH=$(TRILINOS_HOME)/packages
TAGFILES += \
  $(TRILINOS_HOME)/packages/common/tag_files/stratimikosBrowser.tag=$(TRILINOS_HOME)/packages/stratimikos/browser/doc/html
