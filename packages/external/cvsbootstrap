#! /bin/sh
## This version of bootstrap should be used before checking in changes to
## the external package.
set -x
# Only run aclocal if we need to create aclocal.m4
aclocal -I config 
# autoheader is smart and doesn't change anything unless it's necessary
#autoheader 
automake --foreign --add-missing --copy
autoconf
#We always copy the bootstrap output to backup/ so that if someone tries
#to use the sed script more than once, we know what sting to replace with
#the new input.  The default package name is always "sample".
#We need a default because of make dist.
cp configure backup/configure
cp Makefile.in backup/Makefile.in
cp configure.ac backup/configure.ac
cp Makefile.am backup/Makefile.am

