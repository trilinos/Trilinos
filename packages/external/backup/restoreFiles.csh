#!/bin/csh
#Simply copies the CVS versions of the files back to the main external directory

cp configure.ac ../configure.ac
cp Makefile.am ../Makefile.am
cp Makefile.in ../Makefile.in
cp configure ../configure

