#
# This group defines the standard set of Intrepid's tests.
#

(FRAMEWORK, INSTALL) {
    DIRS = Cubature RealSpace;
    ARGS = PrintItAll;
    DESCEND_DIRS = YES;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = SERIAL;
}

(FRAMEWORK, INSTALL) {
    DIRS = Cubature RealSpace;
    ARGS = PrintItAll;
    DESCEND_DIRS = YES;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = CUSTOM-MPI;
}
