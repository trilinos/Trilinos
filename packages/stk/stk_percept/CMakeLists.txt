
ADD_SUBDIRECTORY(stk_percept)

IF(  TPL_ENABLE_Netcdf)
  IF (NOT CMAKE_BUILD_TYPE STREQUAL DEBUG)
    PACKAGE_ADD_TEST_DIRECTORIES(unit_tests)
#    PACKAGE_ADD_TEST_DIRECTORIES(regression_tests)
  ENDIF()
ENDIF()
