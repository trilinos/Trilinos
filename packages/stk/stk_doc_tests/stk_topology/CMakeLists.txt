
INCLUDE(TribitsAddExecutableAndTest)

ASSERT_DEFINED(PACKAGE_SOURCE_DIR CMAKE_CURRENT_SOURCE_DIR)

INCLUDE_DIRECTORIES(${${PACKAGE_NAME}_BINARY_DIR}/stk_topology)

FILE(GLOB SOURCES *.cpp)

TRIBITS_ADD_EXECUTABLE_AND_TEST(
    topology_DocTest
    SOURCES ${SOURCES}
    DEPLIBS stk_topology stk_io stk_mesh_base
    COMM serial mpi
    NUM_MPI_PROCS 1
    )

