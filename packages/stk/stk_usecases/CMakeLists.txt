INCLUDE(PackageMacros)

ADD_SUBDIRECTORY(util)

IF( ${PACKAGE_NAME}_ENABLE_SEACASIoss)
  ADD_SUBDIRECTORY(app)
  ADD_SUBDIRECTORY(io)
  ADD_SUBDIRECTORY(mesh)
ENDIF()

IF( STK_ENABLE_SEACASIoss AND STK_ENABLE_FEI AND STK_ENABLE_AztecOO )
  ADD_SUBDIRECTORY(linsys)
ENDIF()

#IF( STK_ENABLE_ThreadPool AND TPL_ENABLE_MPI )
#  ADD_SUBDIRECTORY(transfer)
#ENDIF()
