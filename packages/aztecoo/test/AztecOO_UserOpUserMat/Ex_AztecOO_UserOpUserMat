#!/bin/csh
../../example/AztecOO_UserOpUserMat/Poisson2d.exe 21 21 >& log.txt
if ("$1" == "-v") then
  cat log.txt
endif
grep "Solver converged" log.txt >> log.txt
if( "$status" != "0" ) then
  echo "  ******** Test failed ********"
else 
  echo "******** Test passed ********"
endif
rm -f log.txt
