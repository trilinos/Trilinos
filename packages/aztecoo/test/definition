#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL, AztecOOThyraTests) {
    TESTS = ../thyra/test/test_aztecoo_thyra_driver.exe;
    ARGS = --matrix-dir=$ABS_TOP_SRCDIR/packages/amesos/test/Test_Basic --show-all-tests;
    X-HOST = paunchy;
    COMM = SERIAL;
}

(FRAMEWORK, AztecOOTests) {
    DIRS = AztecOO,
	         AztecOO_SetParameters;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

(FRAMEWORK, AztecOOExamples) {
    DIRS = ../example/AztecOO_UserOpUserMat;
    ARGS = 21 21;
    EXIT-STATUS = STRING-COMPARISON(Solver converged);
    COMM = SERIAL, MPI;
}

(FRAMEWORK, AztecOOExamples) {
    DIRS = ../example/UG_Ex1;
    COMM = SERIAL, MPI;
}

(FRAMEWORK, INSTALL, AztecOOPyExamples) {
    DIRS = ../python/example;
    COMM = SERIAL, MPI(1-4);
}
