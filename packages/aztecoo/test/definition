#
# FRAMEWORK group
#

# Note, this test will only work if Amesos is enabled!  ToDo: In need to change
# this so that these matrices are copied to this directory if amesos is not
# enabled!
(FRAMEWORK, INSTALL, AztecOOThyraTests) {
    TESTS = ../thyra/test/test_aztecoo_thyra_driver.exe;
    ARGS = --matrix-dir=../../amesos/test/Test_Basic --show-all-tests;
#    ARGS = --matrix-dir=$ABS_TOP_SRCDIR/packages/amesos/test/Test_Basic --show-all-tests;
    X-HOST = paunchy;
    COMM = SERIAL;
}

(FRAMEWORK, AztecOOTests) {
    DIRS = AztecOO, AztecOO_StatusTest, AztecOO_SetParameters;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

(FRAMEWORK, INSTALL, AztecOOTests) {
    DIRS = GMRES_Tests;
    EXIT-STATUS = STRING-COMPARISON(All tests passed);
    COMM = SERIAL, MPI(1 8);
}

(FRAMEWORK, INSTALL, AztecOOExamples) {
    DIRS = ../example/AztecOO_UserOpUserMat;
    ARGS = 21 21;
    EXIT-STATUS = STRING-COMPARISON(Solver converged);
    COMM = SERIAL, MPI;
}

(FRAMEWORK, INSTALL, AztecOOExamples) {
    DIRS = ../example/UG_Ex1;
    COMM = SERIAL, MPI;
}

(FRAMEWORK, INSTALL, AztecOOPyTests) {
    DIRS = ../python/test;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, INSTALL, AztecOOPyExamples) {
    DIRS = ../python/example;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = SERIAL, MPI(1-4);
}
