#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL) {
    TESTS= simple_test/pamgen_test_driver.exe ;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

(DOCUMENTATION) {
    TESTS = ../example/generate-sample-output.pl;
    COMM = SERIAL;
}
