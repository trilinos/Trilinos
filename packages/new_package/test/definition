PACKAGE_NAME=NewPackage

#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL) {
    DIRS = Hello_Test, Jambo_Test, test1;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

(DOCUMENTATION) {
    TESTS = ../example/generate-sample-output.pl;
    COMM = SERIAL;
}
