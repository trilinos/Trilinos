#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL) {
    DIRS = Hello_Test, Jambo_Test, test1;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

(DOCUMENTATION) {
    TESTS = ../example/generate-sample-output.pl;
    COMM = SERIAL;
}

(FRAMEWORK, INSTALL, New_PackagePyTests) {
    DIRS = ../python/test;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, INSTALL, New_PackagePyExamples) {
    DIRS = ../python/example;
    COMM = SERIAL, MPI(1-4);
}
