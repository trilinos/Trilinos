#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL) {
    DIRS = Hello_Test, Jambo_Test, test1;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

(FRAMEWORK, INSTALL, New_PackagePyTests) {
    DIRS = ../python/test;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = SERIAL, MPI(1-4);
}
