#
# FRAMEWORK group
#

(FRAMEWORK, EpetraTests) {
    DIRS = BlockMap,
          Comm,
         CrsGraph,
         CrsMatrix,
         RowMatrix,
         CrsRectMatrix,
         Directory,
         FECrsGraph,
         FECrsMatrix,
         FEVbrMatrix,
         FEVector,
         ImportExport,
         IntSerialDense,
         Map,
       # MapColoring, --> removed this, it's been hanging in MPI.
         MultiVector,
         Object,
         RowMatrixTransposer,
         SerialDense,
         SerialSpdDense,
         VbrMatrix,
         Vector;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

(FRAMEWORK, EpetraExamples) {
    DIRS = ../example/my_example;
    COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, EpetraExamples) {
    DIRS = ../example/UG_Ex1,
           ../example/UG_ExMaps,
           ../example/petra_power_method,
           ../example/verySimple;
    ARGS = 100;
    COMM = SERIAL;
}

(FRAMEWORK,INSTALL) {
    TESTS = ../thyra/example/sillyCgSolve_epetra.exe,
            ../thyra/example/sillyPowerMethod_epetra.exe;
    ARGS = --verbose;
    COMM = SERIAL, MPI;
}

(FRAMEWORK,INSTALL,Thyra) {
    TESTS = ../thyra/test/test_epetra_adapters.exe;
    ARGS = --verbose;
    COMM = SERIAL, MPI;
}

(DOCUMENTATION) {
    TESTS = ../thyra/example/generate-sample-output.pl;
    COMM = SERIAL;
}
