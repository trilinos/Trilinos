
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
INCLUDE_DIRECTORIES(${epetra_BINARY_DIR}/src)
INCLUDE_DIRECTORIES(${epetra_SOURCE_DIR}/src)

ADD_DEFINITIONS(-DHAVE_CONFIG_H)

#FILE(GLOB HEADERS *.h)
#FILE(GLOB SOURCES *.cpp)
#LIST(SORT HEADERS)
#LIST(SORT SOURCES)

SET(SOURCES
	cxx_main.cpp
	)

ADD_EXECUTABLE(BasicPerf_test cxx_main.cpp)

IF(ENABLE_MPI)
  INCLUDE_DIRECTORIES(${MPI_INCLUDE_PATH})
  TARGET_LINK_LIBRARIES(BasicPerf_test epetra ${MPI_LIBRARY} ${LAPACK_LIBRARY} ${BLAS_LIBRARY})
ELSE(ENABLE_MPI)
  TARGET_LINK_LIBRARIES(BasicPerf_test epetra ${LAPACK_LIBRARY} ${BLAS_LIBRARY})
ENDIF(ENABLE_MPI)
