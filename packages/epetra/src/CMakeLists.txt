FILE(GLOB HEADERS *.h)
FILE(GLOB SOURCES *.cpp)
LIST(SORT HEADERS)
LIST(SORT SOURCES)

INCLUDE_DIRECTORIES(${epetra_SOURCE_DIR}/src)
INCLUDE_DIRECTORIES(${MPI_INCLUDE_PATH})

ADD_DEFINITIONS(-DEPETRA_MPI -DMPICH_IGNORE_CXX_SEEK)

ADD_LIBRARY(epetra ${HEADERS} ${SOURCES})

