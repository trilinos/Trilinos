
OPTION(USE_PYTHON_C_API_TO_SET_CATALYST_PYTHON_PATHS
    "Set Python system path for VTK and ParaView zip files using Python C API" ON)

STRING(CONCAT CATALYST_PYTHON_PARAVIEW_ZIP ${ParaView_PREFIX_PATH} "/"
  ${PARAVIEW_PYTHONPATH} "/_paraview.zip")

STRING(CONCAT CATALYST_PYTHON_VTK_ZIP ${ParaView_PREFIX_PATH} "/"
  ${PARAVIEW_PYTHONPATH} "/_vtk.zip")

IF(USE_PYTHON_C_API_TO_SET_CATALYST_PYTHON_PATHS)
  SET(USE_PYTHON_C_API 1)
ELSE()
  SET(USE_PYTHON_C_API 0)
ENDIF()

CONFIGURE_FILE(CatalystPythonPaths.h.in CatalystPythonPaths.h @ONLY)
