INCLUDE(TribitsSubPackageMacros)
INCLUDE(TribitsAddExecutable)
INCLUDE(TribitsLibraryMacros)

TRIBITS_SUBPACKAGE(Conjoin)

INCLUDE_DIRECTORIES(
  "${CMAKE_CURRENT_SOURCE_DIR}"
)

SET(CODE conjoin)
SET(HEADERS "")
SET(SOURCES CJ_ExodusFile.C  CJ_Internals.C     CJ_SystemInterface.C)

TRIBITS_ADD_LIBRARY(${CODE}_app_lib SOURCES ${SOURCES} DEPLIBS exodus suplib)
TRIBITS_ADD_EXECUTABLE(${CODE} NOEXEPREFIX NOEXESUFFIX SOURCES Conjoin.C DEPLIBS ${CODE}_app_lib COMM serial)
install_executable(${CODE})

TRIBITS_SUBPACKAGE_POSTPROCESS()

