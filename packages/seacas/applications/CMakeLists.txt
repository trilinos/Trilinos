ADD_OPTION_AND_DEFINE(${PACKAGE_NAME}_ENABLE_APREPRO
  HAVE_SEACAS_APREPRO
  "Enable support for aprepro in ${PACKAGE_NAME}."
  ON )

ADD_OPTION_AND_DEFINE(${PACKAGE_NAME}_ENABLE_EPU
  HAVE_SEACAS_EPU
  "Enable support for EPU in ${PACKAGE_NAME}."
  ON )

ADD_OPTION_AND_DEFINE(${PACKAGE_NAME}_ENABLE_CONJOIN
  HAVE_SEACAS_CONJOIN
  "Enable support for CONJOIN in ${PACKAGE_NAME}."
  ON )

ADD_OPTION_AND_DEFINE(${PACKAGE_NAME}_ENABLE_EJOIN
  HAVE_SEACAS_EJOIN
  "Enable support for EJOIN in ${PACKAGE_NAME}."
  ON )

ADD_OPTION_AND_DEFINE(${PACKAGE_NAME}_ENABLE_EXO2MAT
  HAVE_SEACAS_EXO2MAT
  "Enable support for EXO2MAT in ${PACKAGE_NAME}."
  ON )

ADD_OPTION_AND_DEFINE(${PACKAGE_NAME}_ENABLE_MAT2EXO
  HAVE_SEACAS_MAT2EXO
  "Enable support for MAT2EXO in ${PACKAGE_NAME}."
  ON )

ADD_OPTION_AND_DEFINE(${PACKAGE_NAME}_ENABLE_EXOMATLAB
  HAVE_SEACAS_EXOMATLAB
  "Enable support for EXOMATLAB in ${PACKAGE_NAME}."
  ON )

ADD_OPTION_AND_DEFINE(${PACKAGE_NAME}_ENABLE_EXODIFF
  HAVE_SEACAS_EXODIFF
  "Enable support for exodiff in ${PACKAGE_NAME}."
  ON )

ADD_OPTION_AND_DEFINE(${PACKAGE_NAME}_ENABLE_NEM_SLICE
  HAVE_SEACAS_NEM_SLICE
  "Enable support for nem_slice in ${PACKAGE_NAME}."
  ON )

ADD_OPTION_AND_DEFINE(${PACKAGE_NAME}_ENABLE_NEM_SPREAD
  HAVE_SEACAS_NEM_SPREAD
  "Enable support for nem_spread in ${PACKAGE_NAME}."
  ON )

#
# C) Add the libraries, tests, and examples
#

ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_APREPRO)
IF (${PACKAGE_NAME}_ENABLE_APREPRO)
ADD_SUBDIRECTORY(aprepro)
ENDIF()

ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_EPU)
IF (${PACKAGE_NAME}_ENABLE_EPU)
ADD_SUBDIRECTORY(epu)
ENDIF()

ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_CONJOIN)
IF (${PACKAGE_NAME}_ENABLE_CONJOIN)
ADD_SUBDIRECTORY(conjoin)
ENDIF()

ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_EJOIN)
IF (${PACKAGE_NAME}_ENABLE_EJOIN)
ADD_SUBDIRECTORY(ejoin)
ENDIF()

ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_EXO2MAT)
IF (${PACKAGE_NAME}_ENABLE_EXO2MAT)
ADD_SUBDIRECTORY(exo2mat)
ENDIF()

IF(TPL_ENABLE_MATLAB)
ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_MAT2EXO)
IF (${PACKAGE_NAME}_ENABLE_MAT2EXO)
ADD_SUBDIRECTORY(mat2exo)
ENDIF()
ENDIF()

ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_EXOMATLAB)
IF (${PACKAGE_NAME}_ENABLE_EXOMATLAB)
ADD_SUBDIRECTORY(exomatlab)
ENDIF()

ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_EXODIFF)
IF (${PACKAGE_NAME}_ENABLE_EXODIFF)
ADD_SUBDIRECTORY(exodiff)
ENDIF()

ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_NEM_SLICE)
IF (${PACKAGE_NAME}_ENABLE_NEM_SLICE)
ADD_SUBDIRECTORY(nem_slice)
ENDIF()

ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_NEM_SPREAD)
IF (${PACKAGE_NAME}_ENABLE_NEM_SPREAD)
ADD_SUBDIRECTORY(nem_spread)
ENDIF()
