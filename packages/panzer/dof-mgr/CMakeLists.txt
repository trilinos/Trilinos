TRIBITS_SUBPACKAGE(DofMgr)

SET(PANZER_HAVE_EPETRA ${${PACKAGE_NAME}_ENABLE_Epetra} )

IF(PANZER_HAVE_EPETRA)
   MESSAGE(STATUS "Global Indexer Epetra Utilities On")
ELSE()
   MESSAGE(STATUS "Global Indexer Epetra Utilities Off")
ENDIF()

ADD_SUBDIRECTORY(src)

TRIBITS_ADD_TEST_DIRECTORIES(test)
TRIBITS_ADD_EXAMPLE_DIRECTORIES(example)

TRIBITS_SUBPACKAGE_POSTPROCESS()

# TOUCH (to trigger reconfigure)
