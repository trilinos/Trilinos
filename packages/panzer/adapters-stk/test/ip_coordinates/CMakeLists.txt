

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})
INCLUDE_DIRECTORIES(${${PARENT_PACKAGE_NAME}_SOURCE_DIR}/disc-fe/test/equation_set)
INCLUDE_DIRECTORIES(${${PARENT_PACKAGE_NAME}_SOURCE_DIR}/disc-fe/test/closure_model)
INCLUDE_DIRECTORIES(${${PACKAGE_NAME}_SOURCE_DIR}/test/response_library)
INCLUDE_DIRECTORIES(${${PACKAGE_NAME}_SOURCE_DIR}/test/bcstrategy)

SET(UNIT_TEST_DRIVER
  ${PANZER_UNIT_TEST_MAIN})

IF(PANZER_HAVE_EPETRA)
  SET(SOURCES epetra_ip_coordinates.cpp)
ELSE(PANZER_HAVE_EPETRA)
  SET(SOURCES tpetra_ip_coordinates.cpp)
ENDIF(PANZER_HAVE_EPETRA)

TRIBITS_ADD_EXECUTABLE_AND_TEST(
  ip_coordinates
  SOURCES ${SOURCES} ${UNIT_TEST_DRIVER}
  COMM serial mpi
  NUM_MPI_PROCS 2
  )
