#
# FRAMEWORK group
#

# Sacado unit tests
(FRAMEWORK,INSTALL) {
    DIRS = TestSuite, tradoptest ;
    EXIT-STATUS = STRING-COMPARISON(OK);
    COMM = SERIAL, MPI;
}

# Sacado examples
(FRAMEWORK,INSTALL) {
    DIRS = ../example ;
    COMM = SERIAL;
}
