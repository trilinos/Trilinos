

SET(HEADERS "")
SET(SOURCES "")

TRIBITS_INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

APPEND_SET(HEADERS
  LOCA_LAPACK.H
  LOCA_LAPACK_Group.H
  LOCA_LAPACK_Interface.H
  LOCA_Eigensolver_DGGEVStrategy.H
  LOCA_BorderedSolver_LAPACKDirectSolve.H
  LOCA_LAPACK_Factory.H
  )

APPEND_SET(SOURCES
  LOCA_LAPACK_Group.C
  LOCA_Eigensolver_DGGEVStrategy.C
  LOCA_BorderedSolver_LAPACKDirectSolve.C
  LOCA_LAPACK_Factory.C
  )

IF(NOX_ENABLE_ABSTRACT_IMPLEMENTATION_LAPACK AND NOX_ENABLE_LOCA)
  TRIBITS_ADD_LIBRARY(
    localapack
    HEADERS ${HEADERS}
    SOURCES ${SOURCES}
    DEPLIBS loca noxlapack nox
    )
ENDIF()
