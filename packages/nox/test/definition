#
# FRAMEWORK group
#

# NOX LAPACK tests
(FRAMEWORK) {
    DIRS = lapack/NOX,
           lapack/NOX_Group,
           lapack/NOX_NewTest;
    EXIT-STATUS = STRING-COMPARISON(Test passed!);
    ARGS = -v;
    COMM = SERIAL;
}

# NOX Epetra tests
(FRAMEWORK) {
    DIRS = epetra/1Dfem,
           epetra/NOX_Group,
           epetra/NOX_NewTest,
           epetra/NOX_Vector;
           epetra/FiniteDifferenceColoring;
    EXIT-STATUS = STRING-COMPARISON(Test passed!);
    ARGS = -v;
    COMM = SERIAL, MPI;
}

# NOX Epetra test that is limited to 2 processors
(FRAMEWORK) {
    DIRS = epetra/DS6.5.1;
    EXIT-STATUS = STRING-COMPARISON(Test passed!);
    ARGS = -v;
    COMM = SERIAL, MPI(2);
}

# NOX Python tests
(FRAMEWORK,NOXPyTests) {
    DIRS = ../python/test;
    COMM = SERIAL, MPI(1-4);
}

# NOX Python examples
(FRAMEWORK,NOXPyExamples) {
    DIRS = ../python/example-epetra;
    COMM = SERIAL, MPI(1-4);
}

# LOCA Epetra tests
(FRAMEWORK) {
    DIRS = epetra/LOCA_UnitTests,
           epetra/LOCA_Tcubed,
           epetra/LOCA_Pitchfork;
    EXIT-STATUS = STRING-COMPARISON(All tests passed);
    ARGS = -v;
    COMM = SERIAL, MPI;
    X-ARCH = sun4u;
    X-HOST = paunchy;
}

# LOCA LAPACK tests
(FRAMEWORK) {
    DIRS = lapack/LOCA_UnitTests,
           lapack/LOCA_ChanProblem,
           lapack/LOCA_python;
    EXIT-STATUS = STRING-COMPARISON(All tests passed);
    ARGS = -v;
    COMM = SERIAL;
    X-ARCH = sun4u;
    X-HOST = paunchy;
}
