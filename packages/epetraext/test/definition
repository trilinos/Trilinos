#
# FRAMEWORK group
#

(FRAMEWORK, EpetraExtTests) {
    DIRS = MatrixMatrix,
           Permutation,
           Transpose,
           inout;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

# Removed MapColoring & MatrixMatrix from MPI for now because they always seem to hang.

(FRAMEWORK, EpetraExtTests) {
    DIRS = MatrixMatrix,
           MapColoring;
    ARGS = -v;
    COMM = SERIAL;
}

(FRAMEWORK, EpetraExtPyExamples) {
        DIRS = ../python/example;
        COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, EpetraExtPyTests) {
        DIRS = ../python/test;
        COMM = SERIAL, MPI(1-4);
}
