#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = Permutation,
           Transpose,
           inout;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

# Removed MapColoring & MatrixMatrix from MPI for now because they always seems to hang.

(FRAMEWORK) {
    DIRS = MatrixMatrix,
           MapColoring;
    ARGS = -v;
    COMM = SERIAL;
}
