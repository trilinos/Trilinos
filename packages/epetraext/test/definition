#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = MapColoring,
           MatrixMatrix,
           Permutation,
           Transpose,
           inout;
    ARGS = -v;
    COMM = SERIAL, MPI;
}
