#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = CompareWithAztecOO,
           Container,
           CrsRiluk,
           IC,
           ILU,
	   IlukGraph,
	   OverlappingRowMatrix,
	   Relaxation,
	   SetParameters;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

# Removing PreconditionerFactory from MPI for now because it seems to hang.

(FRAMEWORK) {
    DIRS = PreconditionerFactory;
    ARGS = -v;
    COMM = SERIAL;
}
