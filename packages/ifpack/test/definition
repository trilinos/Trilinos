#
# FRAMEWORK group
#

(FRAMEWORK, IFPACKTests) {
    DIRS = CompareWithAztecOO,
           Container,
           CrsRiluk,
           IC,
           ILU,
	   IlukGraph,
	   OverlappingRowMatrix,
	   Relaxation,
	   SetParameters;
    ARGS = -v;
    COMM = SERIAL, MPI;
}
