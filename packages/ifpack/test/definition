#
# FRAMEWORK group
#

(FRAMEWORK, IFPACKTests) {
    DIRS = CompareWithAztecOO,
           Container,
           CrsRiluk,
           IC,
           ILU,
	   IlukGraph,
	   OverlappingRowMatrix,
           PreconditionerFactory,
	   Relaxation,
	   SetParameters,
           TestAll;
    ARGS = -v;
    COMM = SERIAL, MPI(1-4);
}

# Removing PreconditionerFactory from MPI for now because it seems to hang.

(FRAMEWORK, INSTALL, IFPACKExamples) {
    DIRS = ../example;
    ARGS = -v;
    COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, INSTALL, IFPACKPyTests) {
    DIRS = ../python/test;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, INSTALL, IFPACKPyExamples) {
    DIRS = ../python/example;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = SERIAL, MPI(1-4);
}
