#
# FRAMEWORK group
#

(FRAMEWORK, IFPACKTests) {
    DIRS = CompareWithAztecOO,
           Container,
           CrsRiluk,
           IC,
           ILU,
	   IlukGraph,
	   OverlappingRowMatrix,
	   Relaxation,
           PreconditionerFactory,
	   SetParameters;
    ARGS = -v;
    COMM = SERIAL, MPI(1-4);
}

# Removing PreconditionerFactory from MPI for now because it seems to hang.

(FRAMEWORK, IFPACKExamples) {
    DIRS = ../example;
    ARGS = -v;
    COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, IFPACKPyExamples) {
    DIRS = ../python/example;
    COMM = SERIAL, MPI;
}
