if(LAPACK_LIBRARY_DIRS AND LAPACK_LIBRARIES)
  kokkoskernels_find_imported(LAPACK INTERFACE LIBRARIES ${LAPACK_LIBRARIES} LIBRARY_PATHS ${LAPACK_LIBRARY_DIRS})
elseif(LAPACK_LIBRARIES)
  kokkoskernels_find_imported(LAPACK INTERFACE LIBRARIES ${LAPACK_LIBRARIES})
elseif(LAPACK_LIBRARY_DIRS)
  kokkoskernels_find_imported(LAPACK INTERFACE LIBRARIES lapack LIBRARY_PATHS ${LAPACK_LIBRARY_DIRS})
else()
  find_package(LAPACK REQUIRED)
  kokkoskernels_create_imported_tpl(LAPACK INTERFACE LINK_LIBRARIES ${LAPACK_LIBRARIES})
endif()
