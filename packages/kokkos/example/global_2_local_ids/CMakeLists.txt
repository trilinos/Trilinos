INCLUDE(TribitsAddExecutableAndTest)   

INCLUDE_DIRECTORIES(${CMAKE_CURRENT_BINARY_DIR})
INCLUDE_DIRECTORIES(${CMAKE_CURRENT_SOURCE_DIR})

SET(SOURCES "")

SET(SOURCES
  G2L_Main.cpp 
  )

IF( Kokkos_ENABLE_Cuda )

  CUDA_INCLUDE_DIRECTORIES( ${KokkosCore_INCLUDE_DIRS} )
  CUDA_INCLUDE_DIRECTORIES( ${KokkosContainers_INCLUDE_DIRS} )

  CUDA_COMPILE( OBJECTS_CUDA G2L_Cuda.cu )

  LIST( APPEND SOURCES ${OBJECTS_CUDA} )

ENDIF()

TRIBITS_ADD_EXECUTABLE(
  global_2_local_ids
  SOURCES ${SOURCES}
  COMM serial mpi
  DEPLIBS kokkoscore kokkoscontainers
  )


