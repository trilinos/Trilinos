KOKKOSPATH=../LAMMPS-Kokkos/lib/kokkos/core/src
FLAGS="-I./ -I${KOKKOSPATH} -DKOKKOS_HAVE_CUDA -arch=sm_35 -L${KOKKOSPATH} -lkokkosarraycuda -lcuda -lcudart -O3 -g"
rm *.o
nvcc -c -x cu force.cpp ${FLAGS}
nvcc -c -x cu setup.cpp ${FLAGS}
nvcc -c -x cu neighbor.cpp ${FLAGS}
nvcc -c -x cu main.cpp ${FLAGS}
nvcc *.o ${FLAGS} -o MD.cuda


