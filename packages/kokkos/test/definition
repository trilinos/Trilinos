#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = BaseSparseMultiply,
           BaseSparseSolve,
           DenseMultiVector,
           HbMatrix,
           Permutation;
    ARGS = -v;
    COMM = SERIAL;
}
