set( DIR ${CMAKE_CURRENT_SOURCE_DIR} )

set( HEADERS "" )
set( SOURCES "" )

APPEND_GLOB( HEADERS ${DIR}/*.hpp )
APPEND_SET( SOURCES "${DIR}/ROL2_ParameterList.cpp" )

set( HEADERS ${HEADERS} PARENT_SCOPE )
set( SOURCES ${SOURCES} PARENT_SCOPE )

