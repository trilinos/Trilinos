#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL, AmesosThyraTests) {
    TESTS = ../thyra/test/test_amesos_thyra_driver.exe;
    ARGS = --matrix-dir=../thyra/test --show-all-tests;
#    ARGS = --matrix-dir=$ABS_TOP_SRCDIR/packages/amesos/test/Test_Basic --show-all-tests;
    X-HOST = paunchy;
    COMM = SERIAL;
}

(INSTALL) {   
    TESTS = scripts/daily/mpi/TestKlu.exe,
            scripts/daily/mpi/TestLapack.exe,
            scripts/daily/mpi/TestDscpack.exe,
            scripts/daily/mpi/TestScalapack.exe,
            scripts/daily/mpi/TestTaucs.exe,
            scripts/daily/mpi/TestExamples.exe,
            scripts/daily/mpi/TestMumps.exe,
            scripts/daily/mpi/TestSuperlu.exe,
            scripts/daily/mpi/TestSuperludist.exe;
    COMM = CUSTOM-MPI; 
    ARGS = NightlyTest True True;      
    EXIT-STATUS = STRING-COMPARISON ; 
}

(FRAMEWORK, AmesosTests) {   
    DIRS = scripts/daily/mpi;
    COMM = CUSTOM-MPI; 
    ARGS = NightlyTest True True;      
    EXIT-STATUS = STRING-COMPARISON ; 
}

(FRAMEWORK, AmesosExamples) {
    DIRS = ../example;
    COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, INSTALL, AmesosPyExamples) {
    DIRS = ../python/example;
    COMM = SERIAL, MPI(1-4);
}
