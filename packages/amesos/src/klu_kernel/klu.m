function [L,U,P,info] = klu (A, Q, Control)
% [L,U,p,info] = klu (A, q, Control)
%
% computes L*U = A (p,q).  Control is optional (uses defaults if not
% present).  q is optional (uses q = 1:n if not present).
%
% TODO: help for klu goes here ...

help klu
error ('klu mexFunction not found') ;
