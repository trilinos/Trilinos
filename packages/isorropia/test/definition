#
# FRAMEWORK group
#

(FRAMEWORK, IsorropiaTests) {
   DIRS = utest,
          rebalance_1d_default,
          zoltan;
   ARGS = --v;
   COMM = SERIAL, MPI;
}

(INSTALL, IsorropiaTests) {
   DIRS = utest,
          rebalance_1d_default,
          zoltan;
   ARGS = --v;
   COMM = SERIAL, MPI;
}

(FRAMEWORK, IsorropiaTests) {
   DIRS = mtx;
   ARGS = --f=mtx/simple.mtx;
   COMM = SERIAL, MPI;
}


(INSTALL, IsorropiaTests) {
   DIRS = mtx;
   ARGS = --f=mtx/simple.mtx;
   COMM = SERIAL, MPI;
}

(FRAMEWORK, IsorropiaTests) {
   DIRS = mtx;
   ARGS = --f=mtx/simple_rectangular.mtx;
   COMM = SERIAL, MPI;
}


(INSTALL, IsorropiaTests) {
   DIRS = mtx;
   ARGS = --f=mtx/simple_rectangular.mtx;
   COMM = SERIAL, MPI;
}
