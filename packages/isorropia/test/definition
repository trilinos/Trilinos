#
# FRAMEWORK group
#

(FRAMEWORK, IsorropiaTests) {
   DIRS = utest,
          rebalance_1d_default,
          zoltan;
   ARGS = --v;
   COMM = MPI;
}

(INSTALL, IsorropiaTests) {
   DIRS = utest,
          rebalance_1d_default,
          zoltan;
   ARGS = --v;
   COMM = MPI;
}

(FRAMEWORK, IsorropiaTests) {
   DIRS = mtx;
   ARGS = --v --f=mtx/simple.mtx;
   COMM = MPI;
}


(INSTALL, IsorropiaTests) {
   DIRS = mtx;
   ARGS = --v --f=mtx/simple.mtx;
   COMM = MPI;
}

(FRAMEWORK, IsorropiaTests) {
   DIRS = mtx;
   ARGS = --v --f=mtx/simple_rectangular.mtx;
   COMM = MPI;
}


(INSTALL, IsorropiaTests) {
   DIRS = mtx;
   ARGS = --v --f=mtx/simple_rectangular.mtx;
   COMM = MPI;
}
