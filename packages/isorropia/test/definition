#
# FRAMEWORK group
#

(FRAMEWORK, IsorropiaTests) {
   DIRS = utest,
          mtx,
          rebalance_1d_default,
          zoltan;
   ARGS = --v;
   COMM = SERIAL, MPI;
}

(INSTALL, IsorropiaTests) {
   DIRS = utest,
          mtx,
          rebalance_1d_default,
          zoltan;
   ARGS = --v;
   COMM = SERIAL, MPI;
}

