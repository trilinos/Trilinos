#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = BlockDavidson,
           BlockKrylovSchur,
           ModalSolverUtils,
           MVOPTester;
    EXIT-STATUS = STRING-COMPARISON;
    ARGS = -v;
    COMM = SERIAL, MPI;
}
