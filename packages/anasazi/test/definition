#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = BlockDavidson,
           BlockKrylovSchur,
           LOBPCG,
           ModalSolverUtils,
           MVOPTester;
    EXIT-STATUS = STRING-COMPARISON;
    ARGS = -v;
    COMM = SERIAL, MPI;
}
