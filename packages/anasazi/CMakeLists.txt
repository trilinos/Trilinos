PROJECT(Anasazi)

SET(ANASAZI_VERSION "2.1d")

ADD_DEFINITIONS(-D_SCL_SECURE=0)

OPTION(ANASAZI_ENABLE_TESTS
  "Build Anasazi tests."
  OFF
  )

OPTION(ANASAZI_ENABLE_EXAMPLES
  "Build Anasazi examples."
  OFF
  )

ADD_SUBDIRECTORY(src)
ADD_SUBDIRECTORY(util)

IF(ANASAZI_ENABLE_TESTS)
  ADD_SUBDIRECTORY(test)
ENDIF(ANASAZI_ENABLE_TESTS)

IF(ANASAZI_ENABLE_EXAMPLES)
  ADD_SUBDIRECTORY(example)
ENDIF(ANASAZI_ENABLE_EXAMPLES)

