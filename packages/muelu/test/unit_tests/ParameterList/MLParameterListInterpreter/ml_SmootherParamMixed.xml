<!-- TEST: smoother parameters at top level + level by level -->
<ParameterList name="MultiLevelPreconditioner">

  <Parameter name="smoother: type"   type="string" value="symmetric Gauss-Seidel"/>

  <ParameterList name="smoother: list (level 0)">
    <Parameter name="smoother: sweeps" type="int"    value="1"/>
  </ParameterList>

  <ParameterList name="smoother: list (level 1)">
    <Parameter name="smoother: sweeps" type="int"    value="2"/>
  </ParameterList>

</ParameterList>