# based on experimental code
IF(${PACKAGE_NAME}_ENABLE_Experimental)
  TRIBITS_ADD_EXAMPLE_DIRECTORIES(q2q1)
  IF(${PACKAGE_NAME}_ENABLE_ADDITIVE_VARIANT)
    MESSAGE(STATUS "MG Additive Variant enabled")
    TRIBITS_ADD_EXAMPLE_DIRECTORIES(max/AdditiveMG)
  ENDIF()
  IF(${PACKAGE_NAME}_ENABLE_REGION_SPLITTING)
    MESSAGE(STATUS "MG Region Splitting enabled")
    TRIBITS_ADD_EXAMPLE_DIRECTORIES(max/XpetraSplitting)
  ENDIF()
  ADD_SUBDIRECTORY(regionMG)
  TRIBITS_ADD_EXAMPLE_DIRECTORIES(luc/region_algorithms)
  TRIBITS_ADD_EXAMPLE_DIRECTORIES(semicoarsening)
  TRIBITS_ADD_EXAMPLE_DIRECTORIES(tawiesn/crada)
  TRIBITS_ADD_EXAMPLE_DIRECTORIES(tawiesn/aria)
  TRIBITS_ADD_EXAMPLE_DIRECTORIES(caglusa)
  TRIBITS_ADD_EXAMPLE_DIRECTORIES(graham)
ENDIF()
