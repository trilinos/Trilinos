INCLUDE(TrilinosCreateClientTemplateHeaders)

IF(TPL_ENABLE_MATLAB)
  ADD_SUBDIRECTORY(Common)
  TRIBITS_ADD_TEST_DIRECTORIES(UnsmoothedP)
  TRIBITS_ADD_TEST_DIRECTORIES(JacobiSmooth)
  TRIBITS_ADD_TEST_DIRECTORIES(Brick)
  TRIBITS_ADD_TEST_DIRECTORIES(ReentrantDemo)
  TRIBITS_ADD_TEST_DIRECTORIES(CustomP)
  TRIBITS_ADD_TEST_DIRECTORIES(Evolution)
  TRIBITS_ADD_TEST_DIRECTORIES(ApplyMueLu)

  IF(${PACKAGE_NAME}_ENABLE_Intrepid2)
    TRIBITS_ADD_TEST_DIRECTORIES(PCoarsen)
  ENDIF()
ENDIF()
