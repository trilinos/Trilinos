# test-harness-config

################################################################################
# This file contains the settings to be used by the test-harness
#
# All text after a hash (#) is considered a comment and will be ignored
# The format is:
#     TAG = value [value ...]
# For lists items can also be appended using:
#     TAG += value [value ...]
# Values that contain spaces should be placed between double quotes ("")
#     (not in single quotes and double quotes can't be escaped)
# Line continuation characters (\) are allowed as an alternative to (+=)
#     (for adding more values, not for extending a value)
# Adding duplicate tags with (=) and not (+=) will not block older values, but
#     will instead simply append the new values, synonymous to (+=)
################################################################################

#===============================================================================
# Frequently changed configuration options
#===============================================================================

#-------------------------------------------------------------------------------
# The name of the file in "<TRILINOS_DIR>/testharness/elements-machine"
# containing the machine-dependent configure options for this machine.
#
# - multiple values recognized: NO
# - value required: YES

MACHINE_CONFIG_FILE             = ../../packages/ml/test/testharness/linux-csmr02

#-------------------------------------------------------------------------------
# The name of the file in "<TRILINOS_DIR>/testharness/elements-machine"
# containing the machine-independent mpi configure options for this
# configuration of the test-harness.

# - multiple values recognized: NO
# - value required: YES if MPI_DIR is supplied

MACHINE_MPI_CONFIG_FILE         = 

#-------------------------------------------------------------------------------
# The name of the file in "<TRILINOS_DIR>/testharness/elements-trilinos"
# containing the machine-independent configure options for this configuration
# of the test-harness.
#
# - multiple values recognized: NO
# - value required: YES

TRILINOS_CONFIG_FILE            = ../../packages/ml/test/testharness/trilinos-parmetis

#-------------------------------------------------------------------------------
# Indicate how the test-harness should report results. If the value is EMAIL,
# then emails will be sent to either the script owner, the package regression
# list, or the Trilinos regression list (see other email options below). If the
# value is LOCAL_FILESYSTEM, then no emails will be sent at all and the results
# will be written to testharness/results/. This directory will be blown away at
# the beginning of each run of the testharness that has REPORT_METHOD set to
# LOCAL_FILESYSTEM.
#
# - recognized values: EMAIL LOCAL_FILESYSTEM
# - multiple values recognized: NO
# - value required: YES

REPORT_METHOD                   = LOCAL_FILESYSTEM

#===============================================================================
# Less frequently changed configuration options
#===============================================================================

#-------------------------------------------------------------------------------
# Provide the name of the serial build directory that should be configured,
# compiled and tested by the test harness, or leave blank to indicate that there
# should be no serial build. Directory must be a subdirectory of 'Trilinos/'.
#
# - multiple values recognized: NO
# - value required: NO (unless MPI_DIR is omitted)

SERIAL_DIR                      = trilinos-parmetis

#-------------------------------------------------------------------------------
# Provide the name of the mpi build directory that should be configured,
# compiled and tested by the test harness, or leave blank to indicate that there
# should be no mpi build. Directory must be a subdirectory of 'Trilinos/'.
#
# - multiple values recognized: NO
# - value required: NO (unless SERIAL_DIR is omitted

MPI_DIR                         =

#-------------------------------------------------------------------------------
# (LAM only) path and filename of the file containing the names of the machines
# to be used for parallel jobs. If this file doesn't exist, parallel jobs will
# be run on the local machine only. (If this is the case, make sure
# <HOSTFILE> is removed from the value of MPI_STARTUP_CMD.)
#
# - multiple values recognized: NO
# - value required: NO
# - the absolute path of the Trilinos directly above 'testharness' can be
#   referred to with the value <TRILINOS_DIR>

HOST_FILE                       = <TRILINOS_DIR>/hostfile

#-------------------------------------------------------------------------------
# Specify the command to start up the MPI implementation on this machine.
#
# - multiple values recognized: NO
# - value required: NO
# - the value of the HOST_FILE option can be referred to with the value
#   <HOST_FILE>
#   For example:
#   MPI_STARTUP_CMD                 = "lamboot <HOST_FILE> -v"

MPI_STARTUP_CMD                 = 

#-------------------------------------------------------------------------------
# Specify the command (if any) to shut down the MPI implementation on this
# machine.
#
# - multiple values recognized: NO
# - value required: NO
# For example:
# MPI_SHUTDOWN_CMD                = lamhalt

MPI_SHUTDOWN_CMD                = 

#-------------------------------------------------------------------------------
# Specify the command (and options) for executing an MPI process. 
#
# - multiple values recognized: NO
# - value required: NO
# - The final option MUST be the option that specifies the number of processors,
#   but MUST omit the actual number--the individual tests will provide the
#   number of processors to be used.
#   Some examples:
#   MPIGO_CMD                     = "prun -n "
#   MPIGO_CMD                     = "mpirun -np "

MPIGO_CMD                       = 

#-------------------------------------------------------------------------------
# Give the flags you would like passed to 'make'. These should be given as one
# string, as it will be passed verbatim.
#
# - multiple values recognized: NO
# - value required: NO
# For example:
# MAKE_FLAGS                      = "-j 2"

MAKE_FLAGS                      = 

#-------------------------------------------------------------------------------
# Indicate whether or not the test-harness should attempt to update the CVS
# working copy.
#
# - recognized values: YES NO 
# - multiple values recognized: NO
# - value required: YES

CVS_UPDATE                      = NO

#-------------------------------------------------------------------------------
# CVS command on this system. Note that CVS access must not require a password.
#
# - multiple values recognized: NO
# - value required: YES if CVS_UPDATE is set to YES

CVS_CMD                         = cvs

#-------------------------------------------------------------------------------
# Indicate how report text files should be named. ORDER will result in the files
# being named such that they will sort in the order that they occurred. EVENT
# will result in the files being named such that they will sort according to
# the type of event being reported.
#
# - recognized values: ORDER EVENT
# - multiple values recognized: NO
# - value required: YES if REPORT_METHOD is set to LOCAL_FILESYSTEM

REPORT_NAMES                    = ORDER

#-------------------------------------------------------------------------------
# Indicate how mail should be sent. The unix program sendmail is the default.
#
# - recognized values: sendmail
#                      smtp <mail_server>
# - multiple values recognized: NO (except "smtp" and "<mail_server>")
# - value required: YES if REPORT_METHOD is set to EMAIL

MAIL_METHOD                     = 

#-------------------------------------------------------------------------------
# List the email addresses to which an overall summary of this run of the
# testharness should be sent.
#
# - multiple values recognized: YES
# - value required: NO

SUMMARY_EMAIL                   = 

#-------------------------------------------------------------------------------
# List the email addresses to which all summaries and error messages will be
# sent regardless of where else they also get sent (script owners, regression
# lists).
#
# - multiple values recognized: YES
# - value required: NO

ALL_EMAILS                      = 

#-------------------------------------------------------------------------------
# Use this option to enable/disable sending email to the default recipients.
# YES indicates that emails should be sent to all of the usual places (script
# owners, package lists, and Trilinos lists, in addition to ALL_EMAILS and
# SUMMARY_EMAIL). NO indicates that they should only be sent to ALL_EMAILS
# and SUMMARY_EMAIL
#
# - recognized values: YES NO 
# - multiple values recognized: NO
# - value required: YES

SEND_TO_DEFAULTS                = NO

# end test-harness-config
