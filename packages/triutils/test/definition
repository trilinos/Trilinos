#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL) {
    DIRS = Read;
    DESCEND_DIRS = YES;
    COMM = SERIAL, MPI;
#    COMM = SERIAL;
}

(FRAMEWORK, INSTALL, TriutilsPyTests) {
    DIRS = ../python/test;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, INSTALL, TriutilsPyExamples) {
    DIRS = ../python/example;
    COMM = SERIAL, MPI(1-4);
}
