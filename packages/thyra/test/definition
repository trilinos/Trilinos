#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL) {
    TESTS = ../example/operator_vector/sillyCgSolve_mpi.exe,
            ./operator_vector/test_std_ops.exe;
    ARGS = --verbose;
    COMM = MPI,SERIAL;
}

(FRAMEWORK, INSTALL) {
    TESTS = ./operator_vector/test_composite_linear_ops.exe;
    ARGS = --use-mpi --verbose;
    COMM = MPI,SERIAL;
}

(FRAMEWORK, INSTALL) {
    TESTS = operator_vector/test_product_space.exe,
	    operator_vector/test_std_ops.exe,
	    ../example/operator_vector/sillyCgSolve_serial.exe,
	    ../example/operator_vector/sillyPowerMethod_serial.exe;
    ARGS = --verbose;
    COMM = SERIAL;
}

(FRAMEWORK, INSTALL) {
    TESTS = operator_vector/test_scalar_product.exe,
    ARGS = --verbose;
    X-HOST = paunchy;
    COMM = SERIAL;
}

(FRAMEWORK, INSTALL) {
    TESTS = ../example/operator_solve/silly1DFFT_serial.exe;
    ARGS = --verbose;
    X-HOST = paunchy, stratus0;
    COMM = SERIAL;
}

(FRAMEWORK, INSTALL) {
    TESTS = operator_vector/test_composite_linear_ops.exe;
    ARGS = --use-serial --verbose;
    COMM = SERIAL;
}

(FRAMEWORK, INSTALL) {
    TESTS = ../example/operator_vector/sillyCgSolve_serial.exe;
    ARGS = --unsym-op --diag-scale=1.05 --verbose;
    COMM = SERIAL;
}

(FRAMEWORK, INSTALL, ThyraPyTests) {
    DIRS = ../python/test;
    EXIT-STATUS = STRING-COMPARISON;
    COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, INSTALL, ThyraPyExamples) {
    DIRS = ../python/example;
    COMM = SERIAL, MPI(1-4);
}
