
(FRAMEWORK, FeiTests) {
  TESTS = fei_test.exe;
  ARGS = -d utest_base -test unit_tests;
  COMM = SERIAL, MPI(1);
}

(FRAMEWORK, FeiTests) {
  TESTS = fei_test.exe;
  ARGS = -d ./elemDOF_Aztec -i elemDOF.i;
  COMM = SERIAL, MPI(1-2);
}

(FRAMEWORK, FeiExamples) {
  DESCEND_DIRS = "yes";
  DIRS = ../example/beam;
  TESTS = ./beam.exe;
  ARGS = -i beam_crs_aztecoo.i;
  COMM = SERIAL, MPI(1-4);
}

(FRAMEWORK, FeiExamples) {
  DESCEND_DIRS = "yes";
  DIRS = ../example/poisson;
  TESTS = ./poisson.exe;
  ARGS = -i poisson_crs.i;
  COMM = SERIAL, MPI(1);
}

