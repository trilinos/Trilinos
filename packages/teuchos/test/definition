#
# FRAMEWORK group
#

(FRAMEWORK) {
    TESTS = RefCountPtr/RefCountPtr_test.exe;
    COMM = MPI;
}

(FRAMEWORK) {
    TESTS = Time/Time_test.exe;
    ARGS = -v;
    COMM = MPI;
}

(FRAMEWORK) {
    TESTS = RefCountPtr/RefCountPtr_test.exe,
            Workspace/workspace_test.exe,
            dyn_cast/dyn_cast_test.exe;
    ARGS = --verbose;
    COMM = SERIAL;
}

(FRAMEWORK) {
    TESTS = BLAS/BLAS_test.exe,
            LAPACK/LAPACK_test.exe,
            DenseMatrix/DenseMatrix_test.exe;
    ARGS = -v;
    COMM = SERIAL;
}

(FRAMEWORK) {
    TESTS = ParameterList/ParameterList_test.exe;
    ARGS = --v;
    COMM = MPI,SERIAL;
}
