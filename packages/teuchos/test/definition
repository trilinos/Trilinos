#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = BLAS,
           Containers,
	         DenseMatrix,
	         LAPACK,
	         ParameterList,
	         RefCountPtr,
	       # Time, --> removed this, it's been hanging in MPI.
	         Workspace,
	         XML,
	         dyn_cast;
    ARGS = -v;
    COMM = SERIAL, MPI;
}
