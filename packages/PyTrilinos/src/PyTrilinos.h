#ifndef PYTRILINOS_H
#define PYTRILINOS_H

// This is the top-level PyTrilinos header file.  Currently it only exists
// so the NOX configure script can determine where the PyTrilinos source
// directory is

#endif
