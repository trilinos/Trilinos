#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = Platform,
           Comm,
           Distributor,
           Directory,
           ElementSpace,
           BlockElementSpace,
           ImportExport,
           VectorSpace,
           Vector,
           CisMatrix;
    ARGS = -v;
    COMM = SERIAL, MPI;
}
