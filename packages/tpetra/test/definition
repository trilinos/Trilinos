#
# FRAMEWORK group
#

# (FRAMEWORK, INSTALL) {
#     DIRS = DistObject,
#            ImportExport,
#            Vector,
#            CisMatrix;
#     ARGS = -v;
#     COMM = SERIAL, MPI;
# }

(FRAMEWORK, INSTALL) {
    DIRS = Platform;
    ARGS = --verbose;
    COMM = SERIAL, MPI;
}

(FRAMEWORK, INSTALL) {
    DIRS = Distributor, Map, Directory, ImportExport;
    #ARGS = ;
    COMM = SERIAL, MPI;
}

(TpetraInProgress) {
    DIRS = Vector, MultiVector;
    #ARGS = ;
    COMM = SERIAL, MPI;
}
