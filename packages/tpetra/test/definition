#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL) {
    DIRS = Platform,
           Distributor,
           Directory,
           DistObject,
           ElementSpace,
           ImportExport,
           VectorSpace,
           Vector,
           CisMatrix;
    ARGS = -v;
    COMM = SERIAL, MPI;
}
