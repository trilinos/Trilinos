#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = Platform,
           Comm,
           Distributor,
           Directory,
           DistObject,
           ElementSpace,
           BlockElementSpace,
           ImportExport,
           VectorSpace,
           Vector,
           CisMatrix;
    ARGS = -v;
    COMM = SERIAL, MPI;
}
