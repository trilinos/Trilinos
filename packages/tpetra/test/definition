#
# FRAMEWORK group
#

# (FRAMEWORK, INSTALL) {
#     DIRS = Platform,
#            Distributor,
#            Directory,
#            DistObject,
#            ElementSpace,
#            ImportExport,
#            VectorSpace,
#            Vector,
#            CisMatrix;
#     ARGS = -v;
#     COMM = SERIAL, MPI;
# }

(FRAMEWORK, INSTALL) {
    DIRS = Platform;
    ARGS = --verbose;
    COMM = SERIAL, MPI;
}

(FRAMEWORK, INSTALL) {
    DIRS = Distributor;
    #ARGS = ;
    COMM = SERIAL, MPI;
}
