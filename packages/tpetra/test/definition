#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = Platform,
           Distributor,
           Directory,
           ElementSpace,
           BlockElementSpace,
           ImportExport,
           VectorSpace,
           Vector;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

# Removing CisMatrix from MPI for now because it hangs the testharness.
# (See Bugzilla #1468)

(FRAMEWORK) {
    DIRS = CisMatrix;
    ARGS = -v;
    COMM = SERIAL;
}
