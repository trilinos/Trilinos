#
# FRAMEWORK group
#

# (FRAMEWORK, INSTALL) {
#     DIRS = DistObject,
#            ImportExport,
#            Vector,
#            CisMatrix;
#     ARGS = -v;
#     COMM = SERIAL, MPI;
# }

(FRAMEWORK, INSTALL) {
    DIRS = Platform;
    ARGS = --verbose;
    COMM = SERIAL, MPI;
}

(FRAMEWORK, INSTALL) {
    DIRS = Distributor, Map, Directory, ImportExport, Vector;
    #ARGS = ;
    COMM = SERIAL, MPI;
}
