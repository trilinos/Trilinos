#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL) {
    DIRS = Platform,
           Comm,
           Distributor,
           Directory,
           DistObject,
           ElementSpace,
           ImportExport,
           VectorSpace,
           Vector,
           CisMatrix;
    ARGS = -v;
    COMM = SERIAL, MPI;
}
