#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = Platform,
           Distributor,
           ElementSpace,
           BlockElementSpace,
           ImportExport,
           VectorSpace,
           Vector;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

# Removing CisMatrix from MPI for now because it hangs the testharness.
# (See Bugzilla #1468)

(FRAMEWORK) {
    DIRS = CisMatrix,
           Directory;
    ARGS = -v;
    COMM = SERIAL;
}
