#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = BasicPerfTest,
           BlockElementSpace,
           CisMatrix,
           Comm,
           Directory,
           Distributor,
           ElementSpace,
           ImportExport,
           Platform,
           Traits,
           Vector;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

# Removing VectorSpace from MPI for now because it's been hanging.

(FRAMEWORK) {
    DIRS = VectorSpace;
    ARGS = -v;
    COMM = SERIAL;
}
