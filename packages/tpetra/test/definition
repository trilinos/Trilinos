#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = Platform,
           Comm,
           Distributor,
           Directory,
           ElementSpace,
           BlockElementSpace,
           ImportExport,
           VectorSpace;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

# Removing Vector and CisMatrix from MPI for now because they hang the testharness.

(FRAMEWORK) {
    DIRS = Vector,
           CisMatrix;
    ARGS = -v;
    COMM = SERIAL;
}
