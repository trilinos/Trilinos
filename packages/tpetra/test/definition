#
# FRAMEWORK group
#

(FRAMEWORK) {
    DIRS = Platform,
           Comm,
           Distributor,
           Directory,
           ElementSpace,
           BlockElementSpace,
           VectorSpace;
    ARGS = -v;
    COMM = SERIAL, MPI;
}

# Removing ImportExport, Vector, and CisMatrix from MPI for now because they hang the testharness.

(FRAMEWORK) {
    DIRS = ImportExport,
           Vector,
           CisMatrix;
    ARGS = -v;
    COMM = SERIAL;
}
