#
# FRAMEWORK group
#

(FRAMEWORK, INSTALL, TpetraTests) {
    DIRS = Platform, Distributor, Map, Directory, ImportExport, MultiVector, CrsMatrix;
    EXIT-STATUS = STRING-COMPARISON;
    #ARGS = ;
    COMM = SERIAL, MPI;
}

(TpetraInProgress) {
    DIRS = Vector;
    EXIT-STATUS = STRING-COMPARISON;
    #ARGS = ;
    COMM = SERIAL, MPI;
}
