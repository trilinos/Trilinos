ASSERT_DEFINED(${PACKAGE_NAME}_ENABLE_CUSPARSE)
IF(${PACKAGE_NAME}_ENABLE_CUSPARSE)
  TRIBITS_ADD_EXECUTABLE_AND_TEST(
    CuSparseOperator
    SOURCES CuSparseOperator
    COMM mpi serial
    NUM_MPI_PROCS 1    
    STANDARD_PASS_OUTPUT
    )
ENDIF()
