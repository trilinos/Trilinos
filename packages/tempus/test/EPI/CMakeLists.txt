TRIBITS_INCLUDE_DIRECTORIES(REQUIRED_DURING_INSTALLATION_TESTING
                    ${CMAKE_CURRENT_SOURCE_DIR})

TRIBITS_ADD_EXECUTABLE_AND_TEST(
  EPI
  SOURCES Tempus_EPITest.cpp ${TEMPUS_UNIT_TEST_MAIN}
  TESTONLYLIBS tempus_test_models
  NUM_MPI_PROCS 1
  )

IF (TEMPUS_ENABLE_EPETRA_STACK OR TEMPUS_ENABLE_TPETRA_STACK)
  TRIBITS_ADD_EXECUTABLE_AND_TEST(
    EPI_CDR
    SOURCES Tempus_EPI_CDRTest.cpp ${TEMPUS_UNIT_TEST_MAIN}
    TESTONLYLIBS tempus_test_models
    NUM_MPI_PROCS 1
    )
ENDIF()

TRIBITS_COPY_FILES_TO_BINARY_DIR(Test_EPI_CopyFiles
  DEST_FILES
  Tempus_EPI_SinCos.xml
  Tempus_EPI_VanDerPol.xml
  Tempus_EPI_Reaction.xml
  Tempus_EPI_NonAutoSrc.xml
  Tempus_EPI_LotkaVolterra.xml
  Tempus_RK4_LotkaVolterra.xml
  EXEDEPS EPI
  )

IF (TEMPUS_ENABLE_EPETRA_STACK OR TEMPUS_ENABLE_TPETRA_STACK)
  TRIBITS_COPY_FILES_TO_BINARY_DIR(Test_EPI_CDR_CopyFiles
    DEST_FILES
    Tempus_EPI_CDR.xml
    Tempus_SDIRK_CDR.xml
    EXEDEPS EPI_CDR
    )
ENDIF()
